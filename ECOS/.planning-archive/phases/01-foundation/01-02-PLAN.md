---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Build the base layout shell — app chrome with sidebar navigation, responsive container, and page routing structure.

Purpose: Establish the visual frame and routing that all feature pages (form, dashboard, admin) will render inside.
Output: Working app shell with sidebar navigation, responsive layout, and placeholder route pages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Design reference:**
@~/Documents/GitHub/vanderdev-website/src/App.jsx
@~/Documents/GitHub/vanderdev-website/src/components/Sidebar.jsx

**Key patterns from vanderdev-website:**
- Sidebar left (w-64), content right with overflow-y-auto
- Sidebar has logo/title at top, nav items with active state (orange left border + bg)
- Card pattern: bg-[#0b0b0b] border border-neutral-800 rounded-lg p-6
- Page titles: text-2xl font-bold text-white
- Responsive: sidebar collapses on mobile (hamburger menu or hidden)

**Pages needed (from roadmap phases):**
- Agreement Form (Phase 3) — the main form
- Workflow Status (Phase 4) — signature progress
- Admin Dashboard (Phase 6) — compliance/approvals
- These are placeholder routes now — real content comes later

**Constraints:**
- react-router-dom for routing (installed in 01-01)
- All routes under /ecosform/ base path
- Role switcher will be added in Phase 5 — leave space in sidebar footer for it
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell with sidebar and routing</name>
  <files>src/App.jsx, src/components/Layout.jsx, src/components/Sidebar.jsx, src/pages/AgreementPage.jsx, src/pages/WorkflowPage.jsx, src/pages/DashboardPage.jsx</files>
  <action>
1. **src/components/Sidebar.jsx**:
   - Fixed sidebar, w-64, bg-[#0b0b0b], border-r border-neutral-800, h-screen
   - Top: "ECOS" title in orange with glow-text, "Security Agreement" subtitle in neutral-400
   - Nav items: New Agreement, Workflow Status, Admin Dashboard
   - Each nav item: hover:bg-neutral-800/50, active state with orange left border + orange text + bg-orange-500/10 (mirror vanderdev-website .nav-item.active pattern)
   - Use NavLink from react-router-dom for active state detection
   - Footer area: empty div reserved for role switcher (Phase 5) — add a comment "// Role switcher placeholder — Phase 5"
   - Mobile: hidden by default on screens < lg, toggled via hamburger button

2. **src/components/Layout.jsx**:
   - Flexbox: sidebar fixed left, main content area with ml-64 (offset for sidebar), p-8, overflow-y-auto, min-h-screen
   - Mobile: no margin-left, full width when sidebar hidden
   - Hamburger button visible on mobile only (top-left), toggles sidebar as overlay

3. **src/pages/AgreementPage.jsx**: Placeholder — page title "New Agreement", subtitle "ECOS Security Agreement form will render here", wrapped in card styling.

4. **src/pages/WorkflowPage.jsx**: Placeholder — page title "Workflow Status", subtitle "Signature workflow tracking will render here".

5. **src/pages/DashboardPage.jsx**: Placeholder — page title "Admin Dashboard", subtitle "Compliance and audit dashboard will render here".

6. **src/App.jsx**: BrowserRouter with basename="/ecosform", Routes wrapping Layout component, nested routes for each page. Default redirect / to /agreement.

Use Tailwind exclusively — no inline styles, no CSS modules. Follow vanderdev-website patterns for spacing, colors, typography.
  </action>
  <verify>`npm run dev` — navigate to /ecosform/, /ecosform/agreement, /ecosform/workflow, /ecosform/dashboard. Sidebar shows correct active states. Pages render inside content area.</verify>
  <done>App shell renders with sidebar navigation, three placeholder pages route correctly, active nav states work, mobile hamburger toggles sidebar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App shell with dark theme sidebar, three page routes, responsive layout matching vanderdev.net design language</what-built>
  <how-to-verify>
    1. Run: `npm run dev` (from ECOS/ directory)
    2. Visit: http://localhost:5180/ecosform/
    3. Verify: Dark background (#050505), sidebar on left with "ECOS" in orange glow
    4. Click each nav item — verify page switches, active state shows orange border
    5. Resize to mobile width (<1024px) — sidebar should collapse, hamburger appears
    6. Click hamburger — sidebar overlays content
    7. Confirm: Overall feel matches vanderdev.net aesthetic (dark, clean, orange accents)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All routes render correctly under /ecosform/ base path
- [ ] Sidebar active states match vanderdev-website pattern
- [ ] Responsive behavior works (sidebar collapses on mobile)
- [ ] No console errors or warnings
- [ ] `npm run build` still succeeds
</verification>

<success_criteria>

- App shell renders with correct vanderdev.net design language
- Three placeholder pages accessible via sidebar navigation
- Responsive layout with mobile hamburger menu
- Human approved visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
